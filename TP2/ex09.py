import numpy as np
a = np.linspace(-np.pi/2, np.pi/2, 3)

f = np.sin(a)
print(f)

c = np.cos(a)

print(c)

b = np.arange(3)

d = np.exp(b)

print(d)

e = np.sqrt(b)

print(e)

g = np.abs(b)

print(g)

h = np.log(np.abs(b))

print(h)